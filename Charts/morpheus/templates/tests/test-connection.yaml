apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "morpheus.fullname" . }}-test-connection"
  labels:
    {{- include "morpheus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "morpheus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
